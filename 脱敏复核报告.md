# 脱敏复核报告

## 复核结论
- 品牌词扫描：未发现残留。
- 乱码扫描：未发现残留。
- 内网/私有端点扫描：未发现真实残留。
- 凭证相关词扫描：命中均为取消令牌、类名或第三方解析术语，不含硬编码密钥。
- 明文 http 地址扫描：命中均为 Triangle.NET 等第三方源码注释或公开参考链接。

## 处理记录
- 命名空间、模块名、类名前缀已迁移为中性命名。
- 私有服务地址统一改为 `https://example.com/...` 或模块说明中的占位地址。
- 商业插件、真实资源、缓存、二进制构建产物和业务壳未纳入。
- 已清理 RoadGen/Generators 中残留的乱码注释、Inspector Header 和调试日志；真实地名式示例路径已替换为 `StreamingAssets/GeoToolkitSamples/synthetic-border-height.geojson`，示例中心点改为合成坐标。

## 本次合规修复
- Unity3DTiles 因上游 Caltech 受限许可已移除，`ThirdParty/Unity3DTiles/` 实体代码不再随公开仓库分发。
- `README.md` 已改为中文首屏，原英文入口另存为 `README.en.md`。
- README、`THIRD_PARTY_NOTICES.md` 和 `待确认_本仓摘要.md` 已移除运行时三维瓦片能力叙事，仓库定位改为编辑器导入框架、地形/路网/雷达工具和 64 位容器。
- `LICENSE` 已补充 `Copyright (c) 2026 zhuxb93`。

## 命中复核
### 品牌词
- 无。

### Unity3DTiles 已移除
- 仅保留本报告与确认摘要中的“已移除”说明，无实体代码、README 功能宣称或 NOTICES 署名残留。

### 凭证相关词
- 主要来自 `CancellationToken`、`CancellationTokenSource`、第三方解析函数 `readToken` 等普通术语。
- 无接口密钥、应用密钥、口令、证书或真实访问凭据。

### 明文/内网地址
- 无内网域名、私有源码托管地址、消息代理地址或私有服务地址残留。
- 第三方源码中的公开 `http://` 参考链接保留原样并由第三方许可约束。

## 编译/导入验证
- 结构校验：`package.json` 与 `GeoToolkit.asmdef` 已通过 JSON 解析。
- 本机未检测到 Unity 6000 编辑器安装，因此未做目标版本的完整本地包导入编译；请在 Unity 6000 项目中导入后复核 Splines、Burst、Collections 等依赖解析。
- 当前已完成源码组织、依赖声明、敏感词复核和 git 提交。
